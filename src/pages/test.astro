---
import StaticPage from "@layouts/StaticPage.astro";

const baseProps = { title: "Test page", description: "Welcome to my testpage" };
const contentName =
	Astro.url.pathname === "/" ? "index" : Astro.url.pathname.slice(1);
---

<StaticPage {baseProps} {contentName} />
